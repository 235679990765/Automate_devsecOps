FROM node:20-alpine AS build
WORKDIR /app

COPY package*.json ./
RUN npm ci || npm install

COPY . .
RUN npm run build

FROM node:20-alpine
WORKDIR /app

COPY --from=build /app .

RUN addgroup -S app && adduser -S app -G app
USER app

EXPOSE {{ port }}

CMD ["npm", "start"]
